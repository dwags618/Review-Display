{
  "name": "dylan-wagner",
  "version": "1.0.0",
  "proxy": "http://localhost:5000",
  "private": true,
  "dependencies": {
    "concurrently": "^3.5.1",
    "prop-types": "^15.6.1",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-scripts": "1.1.3",
    "react-table": "^6.8.0"
  },
  "scripts": {
    "start": "yarn concurrently \"yarn run app\" \"yarn run service\"",
    "app": "react-scripts start",
    "service": "node service",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
